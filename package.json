{
  "name": "margince",
  "private": true,
  "description": "Workspace root. pnpm needs a manifest where the workspace is rooted, and the workspace is rooted here so a unit's extensions/<name>/frontend package can be a member. Nothing is built from this file — the SPA is frontend/, and every script still lives there.",
  "packageManager": "pnpm@10.32.1"
}
