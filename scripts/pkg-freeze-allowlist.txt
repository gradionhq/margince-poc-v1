# Ratified published-surface changes (check-pkg-freeze.sh): one exact
# apidiff finding per line, `<package>: <finding>`. An entry only ever
# matches the one ratified change — the next incompatible edit is a new
# finding and fails — and an entry matching nothing fails as stale, so
# this file cannot accumulate permissions. Package removals are never
# allowlistable (deprecate-then-major cycle, ADR-0069 §3).
#
# PR #195 follow-up (margince-foundation#1160): the retention-class
# vocabulary shrinks to the spec-derivable minimum — Bücher/Abschlüsse
# binds on no record type a CRM holds (absent from DEPACK-PARAM-5), so
# the constant leaves the closed set. Ratified pre-freeze while the de
# unit, the only consumer, changes in the same change set. Remove once
# the extensions arc merges to main and the baseline absorbs it.
github.com/gradionhq/margince/backend/pkg/extension/jurisdiction: BooksAndAnnualAccounts: removed
