# Ratified published-surface changes (check-pkg-freeze.sh):
# `<baseline-sha12> <package>: <finding>` — the exact apidiff finding
# BOUND to the merge-base commit it was ratified against (the gate
# prints the ready-to-paste line on failure). The binding is the
# guarantee: once the ratifying PR merges, every future merge-base is a
# different commit, so an entry can never license a later same-text
# finding or pre-authorize a future break; superseded entries warn
# until removed. Package removals are never allowlistable
# (deprecate-then-major cycle, ADR-0069 §3).
#
# PR #195 follow-up (margince-foundation#1160): the retention-class
# vocabulary shrinks to the spec-derivable minimum — Bücher/Abschlüsse
# binds on no record type a CRM holds (absent from DEPACK-PARAM-5), so
# the constant leaves the closed set. Ratified pre-freeze while the de
# unit, the only consumer, changes in the same change set. Remove once
# the extensions arc merges to main and the baseline absorbs it.
f19c666ed270 github.com/gradionhq/margince/backend/pkg/extension/jurisdiction: BooksAndAnnualAccounts: removed
