# Ratified published-surface changes (check-pkg-freeze.sh): one exact
# apidiff finding per line, `<package>: <finding>`. An entry only ever
# matches the one ratified change — the next incompatible edit is a new
# finding and fails — and an entry matching nothing fails as stale, so
# this file cannot accumulate permissions. Package removals are never
# allowlistable (deprecate-then-major cycle, ADR-0069 §3).
#
# PR #195 review: the retention-class vocabulary becomes a typed closed
# set (RetentionClassName constants + preflight validation) — ratified
# pre-freeze while the de unit, the only consumer, changes in the same
# change set. Remove once the extensions arc merges to main and the
# baseline absorbs it.
github.com/gradionhq/margince/backend/pkg/extension/jurisdiction: RetentionClass.Name: changed from string to RetentionClassName
