{
  "_comment": "Declarative branch-protection for `main`, applied via: gh api -X PUT repos/:owner/:repo/branches/main/protection --input infra/branch-protection.json. This mirrors the live setting on `main`. The full CI merge gate plus SonarCloud are required status checks; the craftsmanship gate runs only after the deterministic gates and has no admin override (ADR-0045, enforce_admins). CodeRabbit is intentionally NOT required — it reviews advisorily but does not block (it renamed its check context and a required-but-renamed external tool hard-locked the repo once). No human-approval count is required — this is a single-owner repo, so requiring an approving review would deadlock every PR (an author cannot approve their own). Mandatory review is enforced through the tool checks instead. The required-and-no-override invariants here are structurally enforced by cli/craft/wiring.",
  "required_status_checks": {
    "strict": true,
    "contexts": [
      "deterministic-gates",
      "craftsmanship",
      "craft-residue",
      "integration (RLS + erasure + HTTP e2e)",
      "govulncheck",
      "frontend (biome + vitest + tsc + build)",
      "SonarCloud Code Analysis"
    ]
  },
  "enforce_admins": true,
  "required_pull_request_reviews": null,
  "restrictions": null,
  "required_linear_history": true,
  "allow_force_pushes": false,
  "allow_deletions": false,
  "required_conversation_resolution": true
}
