// Package crmsearch owns retrieval: full-text (tsvector), vector
// (pgvector), and the context-graph assembly (ADR-0007). It implements
// the retrieval seam; crm-ai reaches it only through that interface.
package crmsearch
