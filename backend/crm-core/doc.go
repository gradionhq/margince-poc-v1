// Package crmcore owns the clean relational core: person, organization,
// deal, pipeline/stage, activity, lead, the typed relationship table, and
// the audit log. It implements the sor seam for SoR-mode and the
// connector Sink; its guts live under internal/.
package crmcore
